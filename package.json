{
  "name": "contrastdesk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Check every color pairing in your palette against WCAG 2 contrast thresholds. Preview text and export a report.",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "start": "node scripts/serve.mjs",
    "format": "prettier --write src scripts tests"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "prettier": "3.8.1"
  }
}
